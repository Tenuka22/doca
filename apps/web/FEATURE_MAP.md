# Feature Map — apps/web
_Generated by Agent 1. Do not hand-edit._

## Feature: Auth / Session
**Route(s):** `/sign-in`, `/sign-up`
**Status:** active

### Files
| File | Role | Key imports |
|------|------|-------------|
| `apps/web/src/routes/sign-in.tsx` | Clerk SignIn page | `@clerk/tanstack-react-start`, `createFileRoute` |
| `apps/web/src/routes/sign-up.tsx` | Clerk SignUp page | `@clerk/tanstack-react-start`, `createFileRoute` |
| `apps/web/src/routes/__root.tsx` | Root layout — ClerkProvider, TooltipProvider, Toaster, devtools, auth token bridge | `@clerk/tanstack-react-start`, `@tanstack/react-router`, `@zen-doc/ui`, `@tanstack/react-query-devtools`, `@zen-doc/env/web` |
| `apps/web/src/utils/clerk-auth.ts` | Isomorphic auth token getter + `getMetadataRole()` | `@tanstack/react-start`, `@clerk/tanstack-react-start/server` |
| `apps/web/src/start.ts` | SSR middleware — `clerkMiddleware()` | `@clerk/tanstack-react-start/server`, `@tanstack/react-start` |

### Loader prefetches
| Route file | Query prefetched | queryClient.ensureQueryData? |
|------------|-----------------|------------------------------|
| None | — | — |

### oRPC / Query calls
| Call | File | Hook or inline? |
|------|------|-----------------|
| None | — | — |

### Forms
| File | useForm schema | Submit mutation |
|------|---------------|-----------------|
| None | — | — |

### ⚠️ Extraction Candidates
- `apps/web/src/utils/clerk-auth.ts`: `getMetadataRole()` is duplicated as inline checks in every admin route — extract to shared `useRole()` hook
- No `beforeLoad` auth guard exists — every admin route does manual `useUser()` + role check inline instead of a layout-level guard

---

## Feature: Root / App Shell
**Route(s):** `__root__`
**Status:** active

### Files
| File | Role | Key imports |
|------|------|-------------|
| `apps/web/src/routes/__root.tsx` | Root document shell, ClerkProvider, Toaster, TooltipProvider, devtools | `@clerk/tanstack-react-start`, `@tanstack/react-router`, `@zen-doc/ui`, `@tanstack/react-query-devtools` |
| `apps/web/src/router.tsx` | Router factory — creates TanStack Router with SSR query integration | `@tanstack/react-router`, `@tanstack/react-router-ssr-query`, `routeTree.gen.ts`, `utils/orpc` |
| `apps/web/src/start.ts` | SSR entry — `clerkMiddleware()` | `@tanstack/react-start`, `@clerk/tanstack-react-start/server` |
| `apps/web/src/index.css` | Imports `@zen-doc/ui/globals.css` | — |
| `apps/web/src/components/loader.tsx` | Default pending component (spinner) | `lucide-react` |

### Loader prefetches
None.

### oRPC / Query calls
None.

### Forms
None.

### ⚠️ Extraction Candidates
- `RouterAppContext` defined in `__root.tsx` could move to `router.tsx` for single-responsibility

---

## Feature: Home / Landing Page
**Route(s):** `/`
**Status:** active

### Files
| File | Role | Key imports |
|------|------|-------------|
| `apps/web/src/routes/index.tsx` | Public landing page — shows nav links, sign-in status, user button | `@clerk/tanstack-react-start`, `@tanstack/react-router`, `@zen-doc/ui/components/button`, `@zen-doc/ui/components/card` |

### Loader prefetches
None.

### oRPC / Query calls
None.

### Forms
None.

### ⚠️ Extraction Candidates
- Role-check for admin link is inline (`publicMetadata.role === "admin"`) — should use a shared `useRole()` hook

---

## Feature: Admin Layout
**Route(s):** `/admin`
**Status:** active

### Files
| File | Role | Key imports |
|------|------|-------------|
| `apps/web/src/routes/admin.tsx` | Admin layout — SidebarProvider, AdminSidebar, Outlet | `@clerk/tanstack-react-start`, `@tanstack/react-router`, `@zen-doc/ui/components/sidebar`, `components/admin-sidebar` |
| `apps/web/src/components/admin-sidebar.tsx` | Admin sidebar navigation | `@zen-doc/ui/components/sidebar`, `lucide-react`, `components/sidebar-nav-section`, `components/sidebar-user-footer` |
| `apps/web/src/components/sidebar-nav-section.tsx` | Reusable sidebar nav section with links | `@tanstack/react-router`, `@zen-doc/ui/components/sidebar` |
| `apps/web/src/components/sidebar-user-footer.tsx` | Sidebar footer with UserButton and name | `@clerk/tanstack-react-start`, `@zen-doc/ui/components/sidebar` |

### Loader prefetches
None (layout only).

### oRPC / Query calls
None.

### Forms
None.

### ⚠️ Extraction Candidates
- No `beforeLoad` auth guard on admin layout — routes rely on inline checks
- No role guard (`admin` only) at the layout level

---

## Feature: Admin Dashboard
**Route(s):** `/admin/`
**Status:** active

### Files
| File | Role | Key imports |
|------|------|-------------|
| `apps/web/src/routes/admin/index.tsx` | Admin dashboard — stats cards, area chart, quick-action buttons, pending-doctor count | `@clerk/tanstack-react-start`, `@tanstack/react-query`, `@tanstack/react-router`, `@zen-doc/ui/*`, `recharts`, `lucide-react`, `date-fns`, `utils/clerk-auth`, `utils/orpc` |

### Loader prefetches
| Route file | Query prefetched | queryClient.ensureQueryData? |
|------------|-----------------|------------------------------|
| `admin/index.tsx` | `orpc.stats` via `prefetchQuery({ queryKey: orpc.stats.queryKey(), queryFn: () => orpc.stats.call() })` | No — uses `prefetchQuery` |

### oRPC / Query calls
| Call | File | Hook or inline? |
|------|------|-----------------|
| `orpc.stats.call()` | `admin/index.tsx:144` | Inline `useQuery` |

### Forms
None.

### ⚠️ Extraction Candidates
- `DashboardSkeleton` (lines 47-63), `MetricCard` (lines 66-105), `SectionHeader` (lines 107-125) all duplicated identically across admin dashboard, doctor dashboard, doctor sessions, doctor availability, doctor profile, doctor plans — extract to shared components
- Role check and sign-in check are inlined (lines 148-191) — should be in layout `beforeLoad`
- Uses `prefetchQuery` instead of `ensureQueryData` — inconsistent with canonical pattern
- Inline `useQuery` instead of extracted query hook

---

## Feature: Admin Doctor Approval
**Route(s):** `/admin/doc-requests/`
**Status:** active

### Files
| File | Role | Key imports |
|------|------|-------------|
| `apps/web/src/routes/admin/doc-requests/index.tsx` | List pending doctor applications with search, pagination, approve action | `@clerk/tanstack-react-start`, `@tanstack/react-query`, `@tanstack/react-router`, `@zen-doc/ui/*`, `lucide-react`, `zod`, `utils/clerk-auth`, `utils/orpc` |

### Loader prefetches
| Route file | Query prefetched | queryClient.ensureQueryData? |
|------------|-----------------|------------------------------|
| `admin/doc-requests/index.tsx` | `orpc.pendingDoctors` via `fetchQuery` | No — uses `fetchQuery` (not `ensureQueryData`) |

### oRPC / Query calls
| Call | File | Hook or inline? |
|------|------|-----------------|
| `orpc.pendingDoctors.call(input)` | `admin/doc-requests/index.tsx:107` | Inline `useQuery` |
| `orpc.approveDoctor.mutationOptions()` | `admin/doc-requests/index.tsx:115` | Inline `useMutation` |

### Forms
None (approvals are button clicks, not forms).

### ⚠️ Extraction Candidates
- Inline `PendingDoctor` / `PendingDoctorsPage` interfaces (lines 32-52) — should come from `@zen-doc/api` types
- Pagination + search UI duplicated across admin doc-requests, admin doctors, admin sessions, admin patients, admin plans, admin guardians, admin credits — extract shared `<PaginatedList>` or similar
- Uses `fetchQuery` instead of `ensureQueryData`
- Uses `pendingDoctors.refetch()` instead of `queryClient.invalidateQueries()`
- Duplicate sign-in / role-check blocks (lines 125-179) — should be layout-level

---

## Feature: Admin Doctor Management
**Route(s):** `/admin/doctors/`, `/admin/doctors/$doctorId`
**Status:** active

### Files
| File | Role | Key imports |
|------|------|-------------|
| `apps/web/src/routes/admin/doctors/index.tsx` | List approved doctors with search + pagination | `@clerk/tanstack-react-start`, `@tanstack/react-query`, `@tanstack/react-router`, `@zen-doc/ui/*`, `lucide-react`, `zod`, `utils/clerk-auth`, `utils/orpc` |
| `apps/web/src/routes/admin/doctors/$doctorId.tsx` | Doctor detail view — profile, files, education, approach steps | `@clerk/tanstack-react-start`, `@tanstack/react-query`, `@tanstack/react-router`, `@zen-doc/ui/*`, `lucide-react`, `components/doctors/*`, `utils/doctor/*`, `utils/orpc` |

### Loader prefetches
| Route file | Query prefetched | queryClient.ensureQueryData? |
|------------|-----------------|------------------------------|
| `admin/doctors/index.tsx` | `orpc.approvedDoctors` via `fetchQuery` | No — uses `fetchQuery` |
| `admin/doctors/$doctorId.tsx` | `orpc.getDoctor` via `fetchQuery` | No — uses `fetchQuery` |

### oRPC / Query calls
| Call | File | Hook or inline? |
|------|------|-----------------|
| `orpc.approvedDoctors.call(input)` | `admin/doctors/index.tsx:107` | Inline `useQuery` |
| `orpc.getDoctor.call(input)` | `admin/doctors/$doctorId.tsx:130` | Inline `useQuery` |

### Forms
None.

### ⚠️ Extraction Candidates
- `DoctorProfileView` / `DoctorEducationView` interfaces defined inline (lines 48-75 of `$doctorId.tsx`) — should come from `@zen-doc/api`
- Same `ApprovedDoctor` / `ApprovedDoctorsPage` interfaces duplicated in both `admin/doctors/index.tsx` and `admin/doc-requests/index.tsx`
- Profile display code in `$doctorId.tsx` is very similar to `doctor-profile-card.tsx` — potential dedup

---

## Feature: Admin Session Management
**Route(s):** `/admin/sessions/`, `/admin/session` (test session)
**Status:** active

### Files
| File | Role | Key imports |
|------|------|-------------|
| `apps/web/src/routes/admin/sessions/index.tsx` | List all sessions with pagination + status badges | `@clerk/tanstack-react-start`, `@tanstack/react-query`, `@tanstack/react-router`, `@zen-doc/ui/*`, `date-fns`, `lucide-react`, `zod`, `utils/clerk-auth`, `utils/orpc` |
| `apps/web/src/routes/admin/session.tsx` | Test session creation — generate session ID, join as doctor via LiveKit | `@clerk/tanstack-react-start`, `@tanstack/react-router`, `@zen-doc/ui/*`, `lucide-react`, `components/livekit/video-room`, `utils/orpc` |

### Loader prefetches
| Route file | Query prefetched | queryClient.ensureQueryData? |
|------------|-----------------|------------------------------|
| `admin/sessions/index.tsx` | `orpc.sessions` via `fetchQuery` | No — uses `fetchQuery` |

### oRPC / Query calls
| Call | File | Hook or inline? |
|------|------|-----------------|
| `orpc.sessions.call(input)` | `admin/sessions/index.tsx:114` | Inline `useQuery` |
| `orpc.createTestSession.call()` | `admin/session.tsx:72` | Inline direct `orpc.*.call()` |

### Forms
None.

### ⚠️ Extraction Candidates
- `SessionStatusBadge` component duplicated in `admin/sessions/index.tsx` and `doctor/index.tsx` and `doctor/sessions/index.tsx` — extract to shared component
- `admin/session.tsx` calls `orpc.createTestSession.call()` directly with no mutation hook — should use `useMutation`

---

## Feature: Admin Patients
**Route(s):** `/admin/patients/`
**Status:** active

### Files
| File | Role | Key imports |
|------|------|-------------|
| `apps/web/src/routes/admin/patients/index.tsx` | List registered patients with pagination + onboarding status | `@clerk/tanstack-react-start`, `@tanstack/react-query`, `@tanstack/react-router`, `@zen-doc/ui/*`, `lucide-react`, `zod`, `utils/clerk-auth`, `utils/orpc` |

### Loader prefetches
| Route file | Query prefetched | queryClient.ensureQueryData? |
|------------|-----------------|------------------------------|
| `admin/patients/index.tsx` | `orpc.patients` via `fetchQuery` | No — uses `fetchQuery` |

### oRPC / Query calls
| Call | File | Hook or inline? |
|------|------|-----------------|
| `orpc.patients.call(input)` | `admin/patients/index.tsx:82` | Inline `useQuery` |

### Forms
None.

### ⚠️ Extraction Candidates
- Same pagination pattern duplicated — extract shared paginated table/list component

---

## Feature: Admin Guardians
**Route(s):** `/admin/guardians/`
**Status:** active

### Files
| File | Role | Key imports |
|------|------|-------------|
| `apps/web/src/routes/admin/guardians/index.tsx` | List guardian accounts with pagination | `@clerk/tanstack-react-start`, `@tanstack/react-query`, `@tanstack/react-router`, `@zen-doc/ui/*`, `date-fns`, `lucide-react`, `zod`, `utils/clerk-auth`, `utils/orpc` |

### Loader prefetches
| Route file | Query prefetched | queryClient.ensureQueryData? |
|------------|-----------------|------------------------------|
| `admin/guardians/index.tsx` | `orpc.guardians` via `fetchQuery` | No — uses `fetchQuery` |

### oRPC / Query calls
| Call | File | Hook or inline? |
|------|------|-----------------|
| `orpc.guardians.call(input)` | `admin/guardians/index.tsx:79` | Inline `useQuery` |

### Forms
None.

### ⚠️ Extraction Candidates
- Same pagination duplication as other admin list pages

---

## Feature: Admin Plans
**Route(s):** `/admin/plans/`
**Status:** active

### Files
| File | Role | Key imports |
|------|------|-------------|
| `apps/web/src/routes/admin/plans/index.tsx` | List all consultation plans with pagination | `@clerk/tanstack-react-start`, `@tanstack/react-query`, `@tanstack/react-router`, `@zen-doc/ui/*`, `lucide-react`, `zod`, `utils/clerk-auth`, `utils/orpc` |

### Loader prefetches
| Route file | Query prefetched | queryClient.ensureQueryData? |
|------------|-----------------|------------------------------|
| `admin/plans/index.tsx` | `orpc.plans` via `fetchQuery` | No — uses `fetchQuery` |

### oRPC / Query calls
| Call | File | Hook or inline? |
|------|------|-----------------|
| `orpc.plans.call(input)` | `admin/plans/index.tsx:82` | Inline `useQuery` |

### Forms
None.

### ⚠️ Extraction Candidates
- Same pagination pattern as other admin list pages

---

## Feature: Admin Credits
**Route(s):** `/admin/credits/`
**Status:** active

### Files
| File | Role | Key imports |
|------|------|-------------|
| `apps/web/src/routes/admin/credits/index.tsx` | List credit transactions with pagination | `@clerk/tanstack-react-start`, `@tanstack/react-query`, `@tanstack/react-router`, `@zen-doc/ui/*`, `date-fns`, `lucide-react`, `zod`, `utils/clerk-auth`, `utils/orpc` |

### Loader prefetches
| Route file | Query prefetched | queryClient.ensureQueryData? |
|------------|-----------------|------------------------------|
| `admin/credits/index.tsx` | `orpc.creditTransactions` via `fetchQuery` | No — uses `fetchQuery` |

### oRPC / Query calls
| Call | File | Hook or inline? |
|------|------|-----------------|
| `orpc.creditTransactions.call(input)` | `admin/credits/index.tsx:84` | Inline `useQuery` |

### Forms
None.

### ⚠️ Extraction Candidates
- Same pagination duplication

---

## Feature: Doctor Layout
**Route(s):** `/doctor`
**Status:** active

### Files
| File | Role | Key imports |
|------|------|-------------|
| `apps/web/src/routes/doctor.tsx` | Doctor layout — SidebarProvider, DoctorSidebar, breadcrumbs, Outlet + `beforeLoad` profile redirect | `@tanstack/react-router`, `@zen-doc/ui/*`, `components/doctor-sidebar`, `utils/orpc` |
| `apps/web/src/components/doctor-sidebar.tsx` | Doctor sidebar navigation | `@zen-doc/ui/components/sidebar`, `lucide-react`, `components/sidebar-nav-section`, `components/sidebar-user-footer` |

### Loader prefetches
None (layout only, but `beforeLoad` fetches doctor profile).

### oRPC / Query calls
| Call | File | Hook or inline? |
|------|------|-----------------|
| `orpc.doctorProfile.call()` | `doctor.tsx:34` | Inline `beforeLoad` fetchQuery |

### Forms
None.

### ⚠️ Extraction Candidates
- `beforeLoad` fetches profile check without `ensureQueryData` pattern — uses bare `fetchQuery`
- `Breadcrumbs` component defined inline (lines 44-86) — could be extracted to shared component
- No auth guard at route level — relies on Clerk session but no explicit redirect to `/sign-in`
- Inline `fetchQuery` rather than canonical `ensureQueryData`

---

## Feature: Doctor Dashboard
**Route(s):** `/doctor/`
**Status:** active

### Files
| File | Role | Key imports |
|------|------|-------------|
| `apps/web/src/routes/doctor/index.tsx` | Doctor dashboard — stats, earnings chart, pending requests, recent activity | `@clerk/tanstack-react-start`, `@tanstack/react-query`, `@tanstack/react-router`, `@zen-doc/ui/*`, `recharts`, `date-fns`, `lucide-react`, `utils/orpc` |

### Loader prefetches
| Route file | Query prefetched | queryClient.ensureQueryData? |
|------------|-----------------|------------------------------|
| `doctor/index.tsx` | `orpc.doctorStats` via `prefetchQuery` | No — uses `prefetchQuery` |
| `doctor/index.tsx` | `orpc.listDoctorSessions` via `prefetchQuery` | No — uses `prefetchQuery` |

### oRPC / Query calls
| Call | File | Hook or inline? |
|------|------|-----------------|
| `orpc.doctorStats.call()` | `doctor/index.tsx:271` | Inline `useQuery` |
| `orpc.listDoctorSessions.call()` | `doctor/index.tsx:276` | Inline `useQuery` |

### Forms
None.

### ⚠️ Extraction Candidates
- `SessionStatusBadge` duplicated — extract to shared component
- `DashboardSkeleton`, `MetricCard`, `SectionHeader` duplicated — extract to shared components
- `PendingRequests` component (lines 173-247) shared pattern with `doctor/sessions/index.tsx` — extract
- All `useQuery` calls inline — should be in query hooks

---

## Feature: Doctor Sessions
**Route(s):** `/doctor/sessions/`, `/doctor/sessions/$sessionId`
**Status:** active

### Files
| File | Role | Key imports |
|------|------|-------------|
| `apps/web/src/routes/doctor/sessions/index.tsx` | Session management — stats, pending requests (approve/reject), upcoming, recent activity | `@clerk/tanstack-react-start`, `@tanstack/react-query`, `@tanstack/react-router`, `@zen-doc/ui/*`, `date-fns`, `recharts`, `lucide-react`, `utils/orpc` |
| `apps/web/src/routes/doctor/sessions/$sessionId.tsx` | Individual session — video room via LiveKit | `@clerk/tanstack-react-start`, `@tanstack/react-query`, `@tanstack/react-router`, `@zen-doc/ui/*`, `lucide-react`, `components/livekit/video-room`, `utils/clerk-auth`, `utils/orpc` |

### Loader prefetches
| Route file | Query prefetched | queryClient.ensureQueryData? |
|------------|-----------------|------------------------------|
| `doctor/sessions/index.tsx` | `orpc.sessionStats` via `prefetchQuery` | No — uses `prefetchQuery` |
| `doctor/sessions/index.tsx` | `orpc.listDoctorSessions` via `prefetchQuery` | No — uses `prefetchQuery` |

### oRPC / Query calls
| Call | File | Hook or inline? |
|------|------|-----------------|
| `orpc.sessionStats.call()` | `doctor/sessions/index.tsx:305` | Inline `useQuery` |
| `orpc.listDoctorSessions.call()` | `doctor/sessions/index.tsx:310` | Inline `useQuery` |
| `orpc.respondSession.mutationOptions()` | `doctor/sessions/index.tsx:177` | Inline `useMutation` inside sub-component |
| `orpc.getLiveKitToken.call(input)` | `doctor/sessions/$sessionId.tsx:23` | Inline `useQuery` |

### Forms
None.

### ⚠️ Extraction Candidates
- `SessionStatusBadge` duplicated third time
- `PendingRequests` component (lines 168-280) similar to doctor dashboard
- `DashboardSkeleton`, `MetricCard`, `SectionHeader` duplicated
- All `useQuery` calls inline
- `doctor/sessions/$sessionId.tsx` uses `getMetadataRole` inline for role inference

---

## Feature: Doctor Availability
**Route(s):** `/doctor/availability`
**Status:** active

### Files
| File | Role | Key imports |
|------|------|-------------|
| `apps/web/src/routes/doctor/availability.tsx` | Weekly availability editor — slot management, bar chart, save/update | `@clerk/tanstack-react-start`, `@tanstack/react-query`, `@tanstack/react-router`, `@zen-doc/ui/*`, `recharts`, `lucide-react`, `sonner`, `utils/orpc` |

### Loader prefetches
| Route file | Query prefetched | queryClient.ensureQueryData? |
|------------|-----------------|------------------------------|
| `doctor/availability.tsx` | `orpc.availabilityStats` via `prefetchQuery` | No — uses `prefetchQuery` |

### oRPC / Query calls
| Call | File | Hook or inline? |
|------|------|-----------------|
| `orpc.availabilityStats.call()` | `doctor/availability.tsx:183` | Inline `useQuery` |
| `orpc.getWeeklyAvailability.call()` | `doctor/availability.tsx:188` | Inline `useQuery` |
| `orpc.listDoctorSessions.call()` | `doctor/availability.tsx:193` | Inline `useQuery` |
| `orpc.saveWeeklyAvailability.mutationOptions()` | `doctor/availability.tsx:217` | Inline `useMutation` |

### Forms
No `useForm` — uses local state for slot data instead of react-hook-form.

### ⚠️ Extraction Candidates
- `MetricCard`, `DashboardSkeleton`, `SectionHeader` duplicated
- Inline `toast` calls instead of shared `notify` util
- All query calls inline — not extracted to hooks
- Uses local state + `useEffect` for form management instead of `useForm` + `zodResolver`
- Heavy inline slot editing logic (200+ lines) — candidate for custom hook

---

## Feature: Doctor Credits / Payouts
**Route(s):** `/doctor/credits`
**Status:** active

### Files
| File | Role | Key imports |
|------|------|-------------|
| `apps/web/src/routes/doctor/credits.tsx` | Credit balance, payout requests, cashout dialog, Stripe Connect link | `@tanstack/react-query`, `@tanstack/react-router`, `@zen-doc/ui/*`, `date-fns`, `lucide-react`, `sonner`, `utils/orpc` |

### Loader prefetches
None.

### oRPC / Query calls
| Call | File | Hook or inline? |
|------|------|-----------------|
| `orpc.getDoctorCredits.call()` | `doctor/credits.tsx:97` | Inline `useQuery` |
| `orpc.requestCashout.mutationOptions()` | `doctor/credits.tsx:101` | Inline `useMutation` |
| `orpc.getConnectAccountStatus.call()` | `doctor/credits.tsx:115` | Inline `useQuery` |
| `orpc.createConnectAccountLink.mutationOptions()` | `doctor/credits.tsx:122` | Inline `useMutation` |

### Forms
No `useForm` — uses local state for cashout amount.

### ⚠️ Extraction Candidates
- `MetricCard` duplicated
- Inline `toast` calls instead of shared `notify` util
- All query calls inline
- No form validation schema — raw input parsing with `Number.parseFloat`

---

## Feature: Doctor Plans
**Route(s):** `/doctor/plans`
**Status:** active

### Files
| File | Role | Key imports |
|------|------|-------------|
| `apps/web/src/routes/doctor/plans.tsx` | Plan management — stats, bar chart, plan cards, create-plan dialog | `@clerk/tanstack-react-start`, `@tanstack/react-query`, `@tanstack/react-router`, `@zen-doc/ui/*`, `recharts`, `lucide-react`, `sonner`, `utils/orpc` |

### Loader prefetches
| Route file | Query prefetched | queryClient.ensureQueryData? |
|------------|-----------------|------------------------------|
| `doctor/plans.tsx` | `orpc.planStats` via `prefetchQuery` | No — uses `prefetchQuery` |
| `doctor/plans.tsx` | `orpc.listDoctorPlans` via `prefetchQuery` | No — uses `prefetchQuery` |

### oRPC / Query calls
| Call | File | Hook or inline? |
|------|------|-----------------|
| `orpc.planStats.call()` | `doctor/plans.tsx:302` | Inline `useQuery` |
| `orpc.listDoctorPlans.call()` | `doctor/plans.tsx:307` | Inline `useQuery` |
| `orpc.createDoctorPlan.mutationOptions()` | `doctor/plans.tsx:167` | Inline `useMutation` inside `CreatePlanDialog` |

### Forms
No `useForm` — uses local state for create-plan dialog.

### ⚠️ Extraction Candidates
- `DashboardSkeleton`, `MetricCard`, `SectionHeader` duplicated
- Inline `toast` calls instead of shared `notify` util
- All query calls inline
- `CreatePlanDialog` is a well-defined sub-component but validation is manual (no Zod schema)

---

## Feature: Doctor Profile
**Route(s):** `/doctor/profile`
**Status:** active

### Files
| File | Role | Key imports |
|------|------|-------------|
| `apps/web/src/routes/doctor/profile.tsx` | Profile page — stats, DoctorProfileCard, DoctorFilesPanel, dev seed | `@clerk/tanstack-react-start`, `@tanstack/react-query`, `@tanstack/react-router`, `@zen-doc/ui/*`, `lucide-react`, `components/doctors/*`, `utils/orpc` |
| `apps/web/src/components/doctors/doctor-profile-card.tsx` | Doctor profile form — multi-tab edit dialog with Zod validation | `@clerk/tanstack-react-start`, `@hookform/resolvers`, `@tanstack/react-query`, `@zen-doc/db/doctor-profile`, `@zen-doc/ui/*`, `react-hook-form`, `zod`, `sonner`, `utils/doctor/*`, `utils/orpc` |
| `apps/web/src/components/doctors/doctor-file-card.tsx` | Individual doctor file display with preview, download, delete | `@zen-doc/ui/*`, `lucide-react` |
| `apps/web/src/components/doctors/doctor-files-panel.tsx` | Doctor files upload/management panel with Dropzone | `@zen-doc/ui/*`, `sonner`, `components/dropzone`, `hooks/doctor/*`, `utils/doctor/files` |
| `apps/web/src/components/doctors/summary-components.tsx` | SummaryItem + SummaryBlock shared display components | `@zen-doc/ui/lib/utils` |
| `apps/web/src/components/doctors/index.ts` | Barrel export for doctor components | — |

### Loader prefetches
| Route file | Query prefetched | queryClient.ensureQueryData? |
|------------|-----------------|------------------------------|
| `doctor/profile.tsx` | `orpc.profileStats` via `prefetchQuery` | No — uses `prefetchQuery` |

### oRPC / Query calls
| Call | File | Hook or inline? |
|------|------|-----------------|
| `orpc.profileStats.call()` | `doctor/profile.tsx:250` | Inline `useQuery` |
| `orpc.doctorProfile.queryOptions()` | `doctor/profile.tsx:255` | Inline `useQuery` (uses `queryOptions` pattern) |
| `orpc.doctorProfile.queryOptions()` | `doctor-profile-card.tsx:138` | Inline `useQuery` (uses `queryOptions` pattern) |
| `orpc.saveDoctorProfile.mutationOptions()` | `doctor-profile-card.tsx:141` | Inline `useMutation` |

### Forms
| File | useForm schema | Submit mutation |
|------|---------------|-----------------|
| `doctor-profile-card.tsx` | `doctorProfileFormSchema` (defined inline in file, lines 85-126) | `saveDoctorProfile.mutate()` |

### ⚠️ Extraction Candidates
- `DashboardSkeleton`, `MetricCard`, `SectionHeader` duplicated (5th occurrence)
- `seedDevProfile` and `seedDevFiles` are dev-only logic mixed into route file — extract to dev util
- `doctorProfileFormSchema` defined inline in component file — should come from `@zen-doc/db/schemas-types`
- `FILE_KIND_COLORS`, `createFakeImageFile` are dev helpers inline
- Multiple inline `toast` calls
- `getDoctorProfileFormValues` helper defined in component file — could be in utils

---

## Feature: Video Room (LiveKit)
**Route(s):** Used by `/admin/session` and `/doctor/sessions/$sessionId`
**Status:** active

### Files
| File | Role | Key imports |
|------|------|-------------|
| `apps/web/src/components/livekit/video-room.tsx` | LiveKit video room — connection, controls, participant list, session timing | `@zen-doc/ui/*`, `lucide-react`, `hooks/use-livekit-room`, `hooks/use-session-timing`, `utils/orpc` |
| `apps/web/src/hooks/use-livekit-room.ts` | LiveKit Room hook — connect, disconnect, audio levels, participant tracking | `livekit-client` |
| `apps/web/src/hooks/use-session-timing.ts` | Session join/leave timing calculation | — |
| `apps/web/src/utils/doctor/types.ts` | ScheduleEntry schema + time constants | `@zen-doc/db/schemas-types/values`, `zod` |

### Loader prefetches
None.

### oRPC / Query calls
| Call | File | Hook or inline? |
|------|------|-----------------|
| `orpc.getLiveKitToken.call()` | `video-room.tsx:565` | Inline inside callback |
| `orpc.recordAttendanceEvent.call()` | `video-room.tsx:62` | Inline in `useAttendanceTracker` |
| `orpc.recordSnapshot.call()` | `video-room.tsx:88` | Inline in `useAttendanceTracker` |
| `orpc.autoMarkAttendance.call()` | `video-room.tsx:108` | Inline in `useAttendanceTracker` |

### Forms
None.

### ⚠️ Extraction Candidates
- `useAttendanceTracker` defined inline in `video-room.tsx` — extract to its own hook
- `formatParticipantLabel`, `AudioVisualizer`, `VideoRoomContent` defined inline
- Dynamic `require()` call (line 654) for Dialog import — should use top-level import

---

## Feature: File Upload / Dropzone
**Route(s):** Used in DoctorFilesPanel (doctor profile)
**Status:** active

### Files
| File | Role | Key imports |
|------|------|-------------|
| `apps/web/src/components/dropzone.tsx` | Reusable drag-and-drop file upload with context | `@zen-doc/ui/components/button`, `@zen-doc/ui/lib/utils`, React |
| `apps/web/src/hooks/doctor/use-doctor-files.ts` | Query hooks for doctor file CRUD | `@tanstack/react-query`, `utils/orpc` |
| `apps/web/src/hooks/doctor/use-doctor-material-preview.ts` | Hook to fetch and create object URL for material preview | `utils/doctor/materials` |
| `apps/web/src/utils/doctor/files.ts` | Doctor file schemas + kind constants | `@zen-doc/db/schemas-types`, `zod` |
| `apps/web/src/utils/doctor/materials.ts` | Isomorphic file fetcher | `@tanstack/react-start`, `utils/orpc` |

### Loader prefetches
None.

### oRPC / Query calls
| Call | File | Hook or inline? |
|------|------|-----------------|
| `orpc.myDoctorFiles.queryOptions()` / `.call()` | `use-doctor-files.ts:7` | In `useDoctorFiles` hook |
| `orpc.createDoctorFile.call()` | `use-doctor-files.ts:24` | In `useUploadDoctorFile` hook |
| `orpc.deleteDoctorFile.call()` | `use-doctor-files.ts:42` | In `useDeleteDoctorFile` hook |
| `orpc.getDoctorFile.call()` | `materials.ts:6` | In isomorphic fn |

### Forms
None (dropzone manages file state locally).

### ⚠️ Extraction Candidates
- `use-doctor-files.ts` already uses proper `queryOptions` pattern — good
- `use-doctor-material-preview.ts` has manual `URL.createObjectURL` management — could use `useQuery` instead

---

## Feature: Shared Hooks & Utilities
**Route(s):** N/A
**Status:** active

### Files
| File | Role | Key imports |
|------|------|-------------|
| `apps/web/src/hooks/use-mobile.ts` | Responsive mobile breakpoint detection | React |
| `apps/web/src/utils/orpc.ts` | oRPC client setup, QueryClient, RPCLink | `@orpc/client`, `@orpc/client/fetch`, `@orpc/server`, `@orpc/tanstack-query`, `@tanstack/react-query`, `@zen-doc/api`, `@zen-doc/env/web`, `sonner`, `utils/clerk-auth` |
| `apps/web/src/lib/utils.ts` | `cn()` utility | `clsx`, `tailwind-merge` |
| `apps/web/src/utils/doctor/calendar.ts` | Calendar grid calculation, event positioning | `date-fns`, `utils/doctor/types` |
| `apps/web/src/utils/doctor/profile-labels.ts` | Label maps for specialty, language, consultation mode, focus area | `@zen-doc/db/doctor-profile` |
| `apps/web/src/utils/doctor/profile-utils.ts` | Parse approach steps and education rows | — |
| `apps/web/src/utils/doctor/index.ts` | Barrel export for doctor utils | — |

### Loader prefetches
None.

### oRPC / Query calls
| Call | File | Hook or inline? |
|------|------|-----------------|
| `orpc` used as base client in all route files | `utils/orpc.ts` | Core setup |

### Forms
None.

### ⚠️ Extraction Candidates
- `utils/doctor/index.ts` is a barrel file — should use direct imports per `AGENTS.md` guidance
- No `notify` util exists (only direct `toast` calls)
- No `useRole()` hook exists
- No shared `formatDate()` util (date-fns `format` called with inconsistent format strings inline)
- No shared `pageContainer` / `cardBase` style constants extracted

---

## Feature: CSS
**Status:** active

### Files
| File | Role |
|------|------|
| `apps/web/src/index.css` | Imports `@zen-doc/ui/globals.css` |
